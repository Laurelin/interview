[project]
name = "leetcode"
version = "0.1.0"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["problems"]
python_files = ["test_*.py"]
